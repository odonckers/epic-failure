name: epic_failure
description: Easily define priorities and error codes for failures used to generate an easy to use failure class.
version: 0.2.0
homepage: https://github.com/odonckers/epic_failure

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  equatable: any
  meta: any

dev_dependencies:
  build_runner: any
  pedantic: any
  test: any
